# Track 40 Plan

- [x] Route narrative mode directly to `NarrativeEngine::generate_risk_prompt`.
- [x] Keep non-narrative ask modes on the generic prompt builder.
- [x] Propagate fallback artifact write failures with context.
- [x] Run full verification gates.
