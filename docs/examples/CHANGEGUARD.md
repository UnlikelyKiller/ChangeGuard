# Project Guidance

- Treat `Cargo.toml`, CI workflows, and migrations as protected paths.
- Prefer targeted verification before whole-repo verification.
- For public API changes, include backward-compatibility notes in the impact review.
- Keep generated state out of version control.
