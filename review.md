**Critical**
- [src/bridge/export.rs](C:/dev/ChangeGuard/src/bridge/export.rs:163), [src/bridge/export.rs](C:/dev/ChangeGuard/src/bridge/export.rs:208), [src/bridge/export.rs](C:/dev/ChangeGuard/src/bridge/export.rs:253), [src/bridge/client.rs](C:/dev/ChangeGuard/src/bridge/client.rs:21), and [src/bridge/notify.rs](C:/dev/ChangeGuard/src/bridge/notify.rs:31) now label ChangeGuard-originated records as `BridgeDirection::Inbound`. Existing bridge contract/tests still treat ChangeGuard emissions as `outbound` ([tests/bridge_tests.rs](C:/dev/ChangeGuard/tests/bridge_tests.rs:23), [tests/bridge_lineage_tests.rs](C:/dev/ChangeGuard/tests/bridge_lineage_tests.rs:9)). `push_risk_alert()` still uses `Outbound` ([src/bridge/notify.rs](C:/dev/ChangeGuard/src/bridge/notify.rs:117)), so the module is internally inconsistent. If AI-Brains routes or filters by direction, core export/query/verify traffic can be misclassified or dropped.

**High**
- [src/bridge/export.rs](C:/dev/ChangeGuard/src/bridge/export.rs:71) does not implement true scoped analysis; it filters the already-materialized global `packet.hotspots` list. That means scoped export can miss in-scope files that were not already in the global top-N. The “scoped” failure-risk branch is also identical to the global branch ([src/bridge/export.rs](C:/dev/ChangeGuard/src/bridge/export.rs:133)), so `failure_risk_probability` is not actually scope-specific despite the C1 requirement.
- [src/verify/ipc_verify.rs](C:/dev/ChangeGuard/src/verify/ipc_verify.rs:38) is effectively dead code. I found no production call sites beyond its own tests; `cli`, `bridge`, `ipc`, and `watch` do not invoke it. Even if wired, it builds a synthetic `ImpactPacket` from raw paths only ([src/verify/ipc_verify.rs](C:/dev/ChangeGuard/src/verify/ipc_verify.rs:67)) and calls `Predictor::predict(&packet, &[])`, so it is not exposing the real predictive verification state promised by C3. It also treats any ledger open/query failure as `drift_detected = false` ([src/verify/ipc_verify.rs](C:/dev/ChangeGuard/src/verify/ipc_verify.rs:123)), which can under-report risk to the capture gate.
- [src/state/storage_cozo.rs](C:/dev/ChangeGuard/src/state/storage_cozo.rs:304), [src/state/storage_cozo.rs](C:/dev/ChangeGuard/src/state/storage_cozo.rs:314), [src/state/storage_cozo.rs](C:/dev/ChangeGuard/src/state/storage_cozo.rs:324), [src/state/storage_cozo.rs](C:/dev/ChangeGuard/src/state/storage_cozo.rs:334), [src/state/storage_cozo.rs](C:/dev/ChangeGuard/src/state/storage_cozo.rs:344), and [src/state/storage_cozo.rs](C:/dev/ChangeGuard/src/state/storage_cozo.rs:354) interpolate external IDs directly into Datalog with `format!`. Any quote in a turn/session/decision/node ID breaks the query, and this also creates a query-injection surface instead of using the parameterized `run_script_with_params()` path already present in the same module.

**Medium**
- [src/bridge/model.rs](C:/dev/ChangeGuard/src/bridge/model.rs:67) adds new payload variants (`Madr`, `RiskAlert`), but [src/bridge/model.rs](C:/dev/ChangeGuard/src/bridge/model.rs:95) keeps `BridgeRecord::VERSION` at `"0.2"`. This is a schema change without a schema-version bump. A peer that understands old `0.2` records but not the new variants or direction semantics has no way to detect the incompatibility up front.
- [src/commands/watch.rs](C:/dev/ChangeGuard/src/commands/watch.rs:158) makes the alert threshold non-configurable in practice. If repo config sets temporal coupling below `0.90`, the function returns early and emits nothing ([src/commands/watch.rs](C:/dev/ChangeGuard/src/commands/watch.rs:166)); when it does emit, it always passes `DEFAULT_RISK_ALERT_THRESHOLD` rather than the configured value ([src/commands/watch.rs](C:/dev/ChangeGuard/src/commands/watch.rs:239)). That does not match the C3 spec’s “configurable threshold (default 90%)”.
- [src/state/storage_cozo.rs](C:/dev/ChangeGuard/src/state/storage_cozo.rs:299) adds six pre-defined cross-domain query helpers even though the C3 design note says AI-Brains should use the existing `run_script` interface rather than pre-defined query endpoints. That is extra API surface in `state/` beyond the track requirement.

**Low**
- Error-path coverage is thin. I did not find tests for quoted/malformed IDs in the Cozo helpers, corrupt/unreadable `ledger.db` in `check_drift()`, invalid/missing `latest-impact.json` in scoped export, or explicit IPC connect/send failure assertions for `push_verify_results()`. The new tests are mostly happy-path and determinism checks in [src/bridge/export.rs](C:/dev/ChangeGuard/src/bridge/export.rs:300), [src/verify/ipc_verify.rs](C:/dev/ChangeGuard/src/verify/ipc_verify.rs:145), and [src/bridge/notify.rs](C:/dev/ChangeGuard/src/bridge/notify.rs:133).

Production-path spot checks:
- I did not find new production `unwrap()`, `expect()`, or `panic!` calls in the requested changed Rust files; matches were in `#[cfg(test)]` sections.
- `notify.rs` is fire-and-forget for both verify results and risk alerts; IPC failures there do not crash the caller.
- I did not see secrets, credentials, API keys, TODOs, or obvious placeholder implementations in the reviewed diff.

ChangeGuard CLI signals were unavailable in this sandbox, so this review is from native diff/source inspection rather than a live `changeguard` run.