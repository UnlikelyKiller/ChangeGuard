No critical findings.

- High: `ledger start` lost the runtime category-correction path and now advertises an unsupported category. `clap` now parses `--category` as `Category` in [src/cli.rs](/abs/path/C:/dev/ChangeGuard/src/cli.rs:577), so aliases and typo-recovery never reach [`resolve_start_category`]( /abs/path/C:/dev/ChangeGuard/src/commands/ledger.rs:33 ) and `run_with` just stringifies the enum into [`execute_ledger_start`]( /abs/path/C:/dev/ChangeGuard/src/cli.rs:957 ). The flexible alias logic still exists in [src/ledger/types.rs](/abs/path/C:/dev/ChangeGuard/src/ledger/types.rs:52) and even tests `doc -> Docs` in [src/ledger/types.rs](/abs/path/C:/dev/ChangeGuard/src/ledger/types.rs:321), but the new integration test now codifies rejecting `doc` in [tests/integration/ledger_cli_parsing.rs](/abs/path/C:/dev/ChangeGuard/tests/integration/ledger_cli_parsing.rs:79). Also, help text now lists `SECURITY` in [src/cli.rs](/abs/path/C:/dev/ChangeGuard/src/cli.rs:577), but the enum has no such variant in [src/ledger/types.rs](/abs/path/C:/dev/ChangeGuard/src/ledger/types.rs:9). This is a real CLI regression.

- High: `ledger status --json --verify-signatures` does not preserve a valid JSON stdout contract. [`execute_ledger_status`]( /abs/path/C:/dev/ChangeGuard/src/commands/ledger.rs:419 ) runs signature verification before entering the JSON branch at [src/commands/ledger.rs](/abs/path/C:/dev/ChangeGuard/src/commands/ledger.rs:429), and [`verify_ledger_signatures`]( /abs/path/C:/dev/ChangeGuard/src/commands/verify.rs:17 ) prints human-readable lines to stdout throughout [src/commands/verify.rs](/abs/path/C:/dev/ChangeGuard/src/commands/verify.rs:31). That means `--json` output can be prefixed with prose before the serialized object at [src/commands/ledger.rs](/abs/path/C:/dev/ChangeGuard/src/commands/ledger.rs:454). The only signature-related test uses `json = false` in [tests/integration/ledger_signature_fix.rs](/abs/path/C:/dev/ChangeGuard/tests/integration/ledger_signature_fix.rs:178), so this path is currently untested.

- Medium: `impact --out` still emits a human success banner on stdout after writing the file. The write path is in [src/commands/impact.rs](/abs/path/C:/dev/ChangeGuard/src/commands/impact.rs:136) and the banner is printed at [src/commands/impact.rs](/abs/path/C:/dev/ChangeGuard/src/commands/impact.rs:149). By contrast, `scan --impact --out` only writes the JSON file and stays stdout-clean in [src/commands/scan.rs](/abs/path/C:/dev/ChangeGuard/src/commands/scan.rs:64). If `--out` is intended as machine mode, these adjacent commands now have inconsistent contracts.

- Medium: the identity-aware env-var comparison itself looks set-based and stable, because extraction sorts and dedups names in [src/index/runtime_usage.rs](/abs/path/C:/dev/ChangeGuard/src/index/runtime_usage.rs:64). The gap is coverage, not the comparison primitive. The new behavior is only asserted by manually injecting `RuntimeUsageDelta` in [tests/integration/risk_analysis.rs](/abs/path/C:/dev/ChangeGuard/tests/integration/risk_analysis.rs:309), while the real enrichment path still only tests “no changes” and “counts unchanged” in [src/impact/enrichment/runtime_usage.rs](/abs/path/C:/dev/ChangeGuard/src/impact/enrichment/runtime_usage.rs:92). There is no end-to-end test covering `git show HEAD:path`, extraction, and persistence of the new `env_vars_previous` / `env_vars_current` fields, so rename/path edge cases and backward-compat defaults remain assumptions.

- Low: stdout/stderr separation for the new progress messages is not actually verified. `ask` writes `Contacting LLM...` to stderr in [src/commands/ask.rs](/abs/path/C:/dev/ChangeGuard/src/commands/ask.rs:281) and `verify` writes its progress line to stderr in [src/commands/verify.rs](/abs/path/C:/dev/ChangeGuard/src/commands/verify.rs:352), which is the right direction, but the tests only check success/failure behavior in [tests/integration/cli_ask.rs](/abs/path/C:/dev/ChangeGuard/tests/integration/cli_ask.rs:12) and [tests/integration/cli_verify.rs](/abs/path/C:/dev/ChangeGuard/tests/integration/cli_verify.rs:4). `cli_impact` also has no coverage for `--json` or `--out` stream behavior in [tests/integration/cli_impact.rs](/abs/path/C:/dev/ChangeGuard/tests/integration/cli_impact.rs:7).

I did not run the test suite or CLI binaries here because the session is read-only. This review is from the diff and surrounding code paths.

