- Medium: The new cache removes regex recompilation, but the hot path still does two hashmap probes on every lookup: `contains_key(symbol)` and then `get(symbol)` in [`src/federated/scanner.rs`](/C:/dev/ChangeGuard/src/federated/scanner.rs:31). For a function introduced specifically to reduce multiplicative overhead in a nested scan, that leaves avoidable work in the tight loop and falls short of the performance intent stated in [`.planning/spec.md`](/C:/dev/ChangeGuard/.planning/spec.md:4) and [`.planning/spec.md`](/C:/dev/ChangeGuard/.planning/spec.md:21). A single-probe `entry`-style lookup would be a better fit for Track E0-3’s goal.

- Medium: The tests do not cover the edge cases the spec and this review scope call out around escaped/special-character symbols and fallback behavior. The updated unit test only exercises plain identifier cases in [`src/federated/scanner.rs`](/C:/dev/ChangeGuard/src/federated/scanner.rs:423), while the spec explicitly requires preserving “substring fallback” and “escape of regex characters” in [`.planning/spec.md`](/C:/dev/ChangeGuard/.planning/spec.md:59) and [`.planning/spec.md`](/C:/dev/ChangeGuard/.planning/spec.md:61). As written, this change could regress handling for symbols containing regex metacharacters or punctuation without any test catching it.

**Open Questions / Assumptions**
- Parameter threading across recursion looks correct. `SymbolMatcher` is instantiated once per scan entry point and passed by `&mut` through recursive descent in [`src/federated/scanner.rs`](/C:/dev/ChangeGuard/src/federated/scanner.rs:264), so I did not find a recursion-state bug.
- I did not run `changeguard` or cargo verification here. In this sandbox, `changeguard doctor/audit/ledger status` failed with `unable to open database file`, so this is a source-and-diff review only.

Overall, the implementation matches the spec structurally: local `SymbolMatcher`, recursive parameter threading, and callsite replacement are all in place. The remaining issues are the incomplete test coverage for the stated behavior guarantees and the still-suboptimal cache lookup path in the hottest loop.